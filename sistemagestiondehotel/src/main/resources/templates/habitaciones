<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Gestión de Habitaciones</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">Hotel UMG</a>
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/clientes">Clientes</a></li>
            <li class="nav-item"><a class="nav-link active" href="/habitaciones">Habitaciones</a></li>
            <li class="nav-item"><a class="nav-link" href="/reservas">Reservas</a></li>
            <li class="nav-item"><a class="nav-link" href="/reportes">Reportes</a></li>
        </ul>
    </div>
</nav>

<div class="container py-4">
    <h2 class="text-center mb-4">Gestión de Habitaciones</h2>

    <!-- Formulario -->
    <form th:action="@{/habitaciones/guardar}" th:object="${habitacion}" method="post" class="card p-4 mb-4">
        <div class="row mb-3">
            <div class="col-md-4">
                <label>Número:</label>
                <input type="text" th:field="*{numero}" class="form-control" required>
            </div>
            <div class="col-md-4">
                <label>Tipo:</label>
                <input type="text" th:field="*{tipo}" class="form-control" required>
            </div>
            <div class="col-md-4">
                <label>Precio:</label>
                <input type="number" th:field="*{precio}" class="form-control" step="0.01" required>
            </div>
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a href="/habitaciones" class="btn btn-secondary">Cancelar</a>
        </div>
    </form>

    <!-- Tabla -->
    <div class="card p-4">
        <h4>Habitaciones Registradas</h4>
        <table class="table table-hover mt-3">
            <thead>
            <tr>
                <th>ID</th><th>Número</th><th>Tipo</th><th>Precio</th><th>Acciones</th>
            </tr>
            </thead>
            <tbody>
            <tr th:each="habitacion : ${habitaciones}">
                <td th:text="${habitacion.id}"></td>
                <td th:text="${habitacion.numero}"></td>
                <td th:text="${habitacion.tipo}"></td>
                <td th:text="${habitacion.precio}"></td>
                <td>
                    <a th:href="@{/habitaciones/editar/{id}(id=${habitacion.id})}" class="btn btn-warning btn-sm">Editar</a>
                    <a th:href="@{/habitaciones/eliminar/{id}(id=${habitacion.id})}" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
            </tbody>
        </table>
    </div>
</div>
</body>
</html>
