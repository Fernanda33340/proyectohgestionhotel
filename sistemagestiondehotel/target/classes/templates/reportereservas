<!DOCTYPE html>
<html lang="es" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Reportes del Sistema</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">Hotel UMG</a>
        <ul class="navbar-nav">
            <li class="nav-item"><a class="nav-link" href="/clientes">Clientes</a></li>
            <li class="nav-item"><a class="nav-link" href="/habitaciones">Habitaciones</a></li>
            <li class="nav-item"><a class="nav-link" href="/reservas">Reservas</a></li>
            <li class="nav-item"><a class="nav-link active" href="/reportes">Reportes</a></li>
        </ul>
    </div>
</nav>

<div class="container py-4">
    <h2 class="text-center mb-4">Reportes Generales</h2>

    <!-- Resumen -->
    <div class="row text-center mb-4">
        <div class="col-md-3">
            <div class="card bg-primary text-white p-3">
                <h4>Total Clientes</h4>
                <h2 th:text="${totalClientes}">0</h2>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-success text-white p-3">
                <h4>Total Reservas</h4>
                <h2 th:text="${totalReservas}">0</h2>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-info text-white p-3">
                <h4>Habitaciones Ocupadas</h4>
                <h2 th:text="${habitacionesOcupadas}">0</h2>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card bg-warning text-white p-3">
                <h4>Ingresos Totales</h4>
                <h2>Q <span th:text="${ingresosTotales}">0.00</span></h2>
            </div>
        </div>
    </div>

    <!-- Tabla de Reservas -->
    <div class="card p-4 mb-4">
        <h4>Listado de Reservas</h4>
        <table class="table table-hover mt-3">
            <thead>
            <tr>
                <th>ID</th><th>Cliente</th><th>Habitación</th><th>Fecha Inicio</th><th>Fecha Fin</th><th>Precio</th>
            </tr>
            </thead>
            <tbody>
            <tr th:each="reserva : ${reservas}">
                <td th:text="${reserva.id}"></td>
                <td th:text="${reserva.cliente.nombre}"></td>
                <td th:text="${reserva.habitacion.numero}"></td>
                <td th:text="${reserva.fechaInicio}"></td>
                <td th:text="${reserva.fechaFin}"></td>
                <td>Q <span th:text="${reserva.habitacion.precio}"></span></td>
            </tr>
            </tbody>
        </table>
    </div>

    <!-- Botón Exportar -->
    <div class="text-center">
        <a href="/reportes/exportar" class="btn btn-outline-success btn-lg">
            📄 Exportar Reporte (PDF)
        </a>
    </div>
</div>

</body>
</html>
